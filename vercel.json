{
  "buildCommand": "npm run build",
  "outputDirectory": ".next",
  "installCommand": "npm ci --prefer-offline --no-audit",
  "framework": "nextjs",
  "functions": {
    "app/**/*.{js,ts,tsx}": {
      "maxDuration": 30
    }
  }
}
